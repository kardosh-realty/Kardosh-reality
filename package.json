{
  "name": "kardosh-realty",
  "private": true,
  "type": "module",
  "description": "Kardosh Realty — Landing (public site) and Dashboard (admin)",
  "main": "index.js",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "node scripts/hostinger-build.mjs",
    "build:combined": "node scripts/hostinger-build.mjs combined",
    "prestart": "node -e \"console.log('[prestart]', { cwd: process.cwd(), port: process.env.PORT, app: process.env.HOSTINGER_APP || '(auto)' })\"",
    "start": "node index.js",
    "dev:landing": "npm run dev --prefix Landing",
    "dev:dashboard": "npm run dev --prefix Dashboard",
    "build:landing": "npm install --prefix Landing && npm run build --prefix Landing && node scripts/copy-landing-dist.mjs",
    "start:landing": "npm run start --prefix Landing",
    "build:dashboard": "npm install --prefix Dashboard && npm run build --prefix Dashboard && node scripts/copy-dashboard-dist.mjs",
    "start:dashboard": "npm run start --prefix Dashboard",
    "install:all": "npm install --prefix Landing && npm install --prefix Dashboard",
    "preview:landing": "npm run preview --prefix Landing",
    "preview:dashboard": "npm run preview --prefix Dashboard"
  }
}
